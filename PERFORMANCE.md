# 前端与访问性能检查

检查日期：2026-09-26。代码基线：`528a236`，包含近期界面重构、专题配图和加载反馈。

## 结论

首页服务端生成很快，当前主要延迟来自公网链路。Cloudflare Tunnel 保留了源站的访问隔离，但不能保证大陆访客与大陆源站之间走大陆内的最短路径。前端优化可以减少传输和交互开销，无法消除这段链路的往返时间。

这些结果来自服务器和当前电脑的有限采样，不代表全国运营商、晚高峰或移动设备。当前电脑 curl 已绕过 HTTP 代理环境变量，但没有验证系统 VPN/TUN 或运营商路由，不能把节点位置推断为所有用户的实际路径。未进行 Lighthouse 评分或真实用户 LCP/INP 分位数统计。

## 实测

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 服务器本机首页首字节 | 8.9-14.2 ms | 4 次请求，入口 `127.0.0.1:8080` |
| 同一服务器访问公网首页首字节 | 564-1539 ms | 4 次请求，经过 Cloudflare |
| 当前电脑访问公网首页首字节 | 2975 ms | 一次直连 curl，`CF-RAY` 后缀为 `AMS` |
| 当前电脑下载已缓存的 JS | 首字节 819-830 ms，总计约 1075 ms | 两次请求；`HIT`、`immutable`、gzip、AMS |
| 首页 HTML | 原始约 40 KB，gzip 约 8 KB | 本地生产构建 |
| 首页初始脚本 | 10 个，gzip 合计约 191 KB | 根据初始 HTML 的脚本引用抓取估算，不含随后预取和缓存复用 |
| 文章列表 HTML | 原始约 37 KB，gzip 约 9 KB | 当前 6 篇文章，包含全部卡片数据 |
| 算法长文 HTML | 原始约 368 KB，gzip 约 32 KB | 约 3640 个标签，代码高亮在服务端完成 |
| 本地配图 `icpc.png` | 142829 字节，2936 px 宽 | 原来每次返回整张图片，不区分专题缩略图与文章封面 |

生产模式构建通过。不存在的文章和专题均返回真实 HTTP 404；即时筛选与浏览器后退可用。已有骨架屏、服务端代码高亮、系统字体、图片懒加载和按源码缓存 MDX 的方向合适。

本次 36 项测试通过，生产构建包含 TypeScript 检查。浏览器确认搜索提交保留暗色状态，连续搜索与后退能恢复相应输入；桌面长文滚动时目录高亮与可见标题一致，手机搜索页无水平溢出。图片条件请求实测从 `200 / 142829 字节` 变为 `304 / 0 字节`。

检查范围不包含低端手机帧率及全国网络质量。后台浏览器中，目录链接更新了 URL hash，但未稳定验证平滑滚动的最终落点；该项仍需前台浏览器确认，不据此宣称所有目录跳转均已通过。

## 本次代码调整

- 本地配图增加 SHA-256 ETag，使用 `private, no-cache, must-revalidate`。每次复用仍验证发布状态和当前内容，相同图片返回无正文的 304。替换、回滚、撤回、弱 ETag 和多个 ETag 的情况有回归测试。这主要节省重复下载流量，仍需要一次网络往返；没有启用 CDN 长期图片缓存。
- 导航进度条只在可见时运行动画，超过 120 ms 才显示，减少快速跳转闪烁及后台动画。
- 即时专题/标签筛选和本地分页关闭多余的 Next.js 预取，普通页面导航保留预取。
- 搜索表单使用 Next.js Form 提交，保留共享页面和客户端状态，避免每次搜索都整页刷新；输入框随查询参数更新，支持浏览器前进后退。
- 目录缓存标题位置，在窗口变化、图片加载或内容展开后重新测量；滚动时用二分查找当前标题。阅读进度合并到每个动画帧一次。
- 文章预览最多 240 个 Unicode 码点，避免首段过长时把大段正文作为卡片数据发送；完整正文与搜索文本保留。
- 复用日期格式器；大封面增加加载优先级；修复封面在 React 初始化前失败时残留破图的情况。

## 下一步优先级

### 1. 验证实际接入路径

在常用的移动、联通、电信网络各测几次首字节时间和下载时间，区分是否经系统代理，以及晚高峰波动。`CF-RAY` 后缀只能说明该次请求的 Cloudflare 节点，不能代表 Tunnel 的全部路由。

如果继续使用当前 Tunnel，接受动态请求的跨境往返，并优先减少请求和资源体积。若目标是大陆用户稳定低延迟，可评估符合接入要求的国内 HTTPS/CDN 路径；需要基础设施选择，不能仅靠调整 React 达到相同效果。迁移到香港等地区的收益也应按运营商实测，不保证天然更快。

Cloudflare China Network 是 Enterprise 客户的独立订阅，需要有效 ICP filing/license 等接入条件；普通 Cloudflare 套餐加 Tunnel 不等于接入中国大陆节点。参见 [官方说明](https://developers.cloudflare.com/china-network/)。

### 2. 图片生成尺寸变体与版本化 URL

为专题图生成约 128 px 的方形变体，为文章卡片生成约 640 px 的变体，大封面按实际展示宽度输出 WebP/AVIF 与 `srcset`。当前一个 56 px 的专题入口可能仍解码整张 2936 px 图片。

若要进一步让 CDN 长时间缓存图片，应同时实现内容哈希 URL、发布/撤回时的清理策略及相应测试。仅把当前 `/media/` 改成一年缓存，会破坏同路径替换和撤回的及时性。即使版本化，也需要明确已缓存旧图的保留政策。

### 3. 保持 HTML 与用户数据缓存边界

`/_next/static/` 已命中长期缓存，无须再强制覆盖其缓存头。不要全站开启 Cache Everything，不要覆盖 `/api/`、登录、个人中心、管理中心或 `/media/` 的现有响应策略。Next.js HTML 和 RSC 对请求头有不同响应，简单按 URL 缓存可能混淆页面与导航数据。

公开正文若增加页面缓存，应和内容发布、回滚、定时文章以及撤回绑定失效；评论、点赞、收藏和会话继续独立动态请求。这个改动应单独设计与验证。

参见 [Cloudflare 默认缓存行为](https://developers.cloudflare.com/cache/concepts/default-cache-behavior/) 与 [HTTP 缓存指令](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control)。

### 4. 文章数量增长后控制列表数据规模

现有即时筛选在 6 篇文章时开销小。文章达到数百篇后，应评估服务端分页或按专题分批加载，不再每次传输全部文章卡片。保留当前立即反馈的体验，但不要为了预取把全部内容提前发送。

长代码文章的 DOM 量明显大于首页，可优先拆分过长文章。进一步使用离屏渲染优化时，要验证目录定位、浏览器页内查找、代码复制和滚动位置，避免直接加入虚拟列表导致功能回退。

### 5. 内容与真实用户验证

本地内容副本仍有已发布的“文章标题 / 一句话概括读者能得到什么。 / 正文从这里开始。”模板文章。建议在内容仓库确认后转为草稿或替换成正式内容。

后续可增加少量匿名性能观测，只采集耗时、页面类别和设备类别，不记录文章搜索词、账户信息或完整 URL 查询参数。先取得真实用户 LCP、INP、CLS 和 TTFB 的分布，再决定是否调整模糊背景、拆分组件或迁移接入链路。

本次未修改生产服务器、Cloudflare 设置和部署拉取方式。代码优化需按正常 GitHub Actions 流程发布后才会在线上生效。
